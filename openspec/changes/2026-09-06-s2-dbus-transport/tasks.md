## 1. Setup

- [x] 1.1 Add `dbus-victron-virtual` and `dbus-native-victron` to `package.json` dependencies (version ranges informed by design.md's Open Questions) - used `^0.1.37`/`^0.4.11`, matching node-red-contrib-victron's own ranges
- [x] 1.2 Add `S2DbusConfigNode` type to `src/types/config-nodes.ts`

## 2. s2-dbus-config

- [x] 2.1 Write tests for `s2-dbus-config` (defaults, field mapping) in `test/nodes/s2-dbus-config.test.ts`. Device type validation moved to `s2-dbus` itself - see design.md ("deviceType validation happens in s2-dbus, not s2-dbus-config")
- [x] 2.2 Implement `src/nodes/s2-dbus-config/index.ts` + `index.html`

## 3. s2-dbus transport node

- [x] 3.0 (added during implementation, see design.md) Extract D-Bus registration into a shared class `S2DbusTransport` (`src/lib/transport/dbus.ts`), mirroring `S2WebSocketTransport`'s shape, with its own tests (`test/lib/dbus-transport.test.ts`) mocking `dbus-native-victron`/`dbus-victron-virtual` directly
- [x] 3.1 Write tests mocking `S2DbusTransport` (pattern: `test/nodes/s2-websocket.test.ts`) covering: transport construction, Connect/Disconnect/Message -> `{command:...}` output parity with `s2-websocket`, `s2Signal`/`S2/0/Active` input handling, unrecognized `deviceType` error status
- [x] 3.2 Write tests for the power-measurement cache: immediate emit on `PowerMeasurementStart` with a cached value, emit on value update while active, no emit after `PowerMeasurementStop`. Superseded by 3.5 below re: D-Bus property exposure - reversed from "no D-Bus property exposed" once requested post-implementation
- [x] 3.3 Implement `src/nodes/s2-dbus/index.ts` + `index.html`. Bugfix from real-device testing (see design.md): D-Bus service naming switched from the (unsanitized, UUID-based) S2 resourceId to the node's own sanitized id - `s2-dbus` no longer takes an `rmConfig` reference at all, since that was its only use
- [x] 3.4 (added post-implementation, see design.md) Claim `DeviceInstance` via `addSettings()`/`com.victronenergy.settings` instead of a user-configured fixed number - matching node-red-contrib-victron's own pattern (retry-with-backoff, response parsing). `s2-dbus-config`'s `deviceInstance` field removed; tests updated in `test/lib/dbus-transport.test.ts`, `test/nodes/s2-dbus-config.test.ts`, `test/nodes/s2-dbus.test.ts`, `test/nodes/s2-resource.test.ts`
- [x] 3.5 (added post-implementation, see design.md) Reverse the "not exposed on D-Bus" decision: `S2DbusTransport` takes a `measurementType` option, declares the corresponding property key(s) at registration time, and exposes `setMeasurementValues()`; `PowerMeasurementCache.update()` now returns both the raw D-Bus-keyed values (always) and the S2-shaped snapshot (only while active). `s2-dbus`/`s2-resource` push every update to the D-Bus property regardless of S2 activity. New tests in `test/lib/dbus-transport.test.ts`, `test/nodes/s2-dbus.test.ts`, `test/nodes/s2-resource.test.ts`

## 4. Integration and docs

- [x] 4.1 Register `s2-dbus`/`s2-dbus-config` in `package.json`'s `node-red.nodes` map
- [x] 4.2 Update `docs/S2_REFERENCE.md`'s transport section to list `s2-dbus` alongside `s2-websocket`
- [x] 4.3 Add an example flow (`examples/s2-dbus-quickstart.json`) demonstrating `s2-rm` + `s2-ombc` wired to `s2-dbus` instead of `s2-websocket`
- [x] 4.4 Run `npm test` and confirm lint + full suite pass, including the `s2-resource` `Transport: D-Bus` integration and the D-Bus BusItem property exposure (task 3.5): 387/387 tests, lint clean
- [x] 4.5 (found via real CEM connection to a deployed `s2-resource`, see design.md) Fix `org.freedesktop.DBus.Error.Failed ['Property S2/0/Active not found in properties.']`: declare `S2/0/Active`/`S2/0/Rm` in `S2DbusTransport`'s `declaration.properties`/`definition` (initial values `0`/`''`), and wire `__s2Handlers.Connect`/`Disconnect` to update `S2/0/Rm` (and reset both on Disconnect) via `setValuesLocally()`. New tests in `test/lib/dbus-transport.test.ts`
- [x] 4.6 Run `npm test` and confirm lint + full suite pass: 392/392 tests, lint clean
- [x] 4.7 (requested for debugging usability, see design.md) Route `s2-dbus`'s (and, for consistency, `s2-websocket`'s) `config.debug`-gated raw S2 message trace from `node.log()` to the Node-RED editor's Debug sidebar via a new shared `publishToDebugSidebar()` helper (`src/lib/node-red-debug-sidebar.ts`, `RED.comms.publish('debug', ...)` - the same channel the core Debug node itself uses), so it's visible without wiring a separate Debug node. Checkbox relabeled "Show raw S2 messages in debug sidebar". New tests in `test/nodes/s2-dbus.test.ts`, `test/nodes/s2-websocket.test.ts`
- [x] 4.8 (requested - Victron UI parity, see design.md) `S2DbusTransport` now always declares the full node-red-contrib-victron-compatible "minimal meter" D-Bus shape (`src/lib/transport/minimal-meter-properties.ts`: Position, NrOfPhases, PhaseSetting, Connected, DeviceType, ErrorCode, IsGenericEnergyMeter, per-phase Ac/L<n>/Current|Energy/Forward|Energy/Reverse|Power|PowerFactor|Voltage, and the top-level Ac/Energy/Forward|Reverse, Ac/Frequency, Ac/Power, Ac/PowerFactor aggregates) alongside the S2-specific properties - not just the measurementType-selected power path(s). `s2-dbus-config` gained `nrOfPhases`/`position`/`phaseSetting` fields (directly settable, matching node-red-contrib-victron's own `acload_*`/`heatpump_*` config fields) feeding it. Numeric paths with no real data source are initialized to `null` (dbus-victron-virtual's "unknown" BusItem convention, wire-encoded as an empty/invalid value) rather than a fabricated `0` - conservative by construction; `measurementType`'s tracked key(s) still get a real `0`-then-live value, overriding the shape's own `null` default for just that key. New tests in `test/lib/dbus-transport.test.ts`, `test/nodes/s2-dbus-config.test.ts`, `test/nodes/s2-dbus.test.ts`, `test/nodes/s2-resource.test.ts`
- [x] 4.9 Run `npm test` and confirm lint + full suite pass: 409/409 tests, lint clean
- [x] 4.10 (found via real use of the new debug sidebar) Two follow-up fixes to task 4.7's debug-sidebar work: (1) every `-> to CEM`/`<- from CEM` topic now includes the actual cemId, so multiple/varying CEMs are distinguishable - outbound topics for `s2-dbus`/`s2-resource`'s D-Bus path track the real D-Bus-supplied cemId in a small closure variable (set on `connect`, cleared on `disconnect`) rather than the internal fixed placeholder every outbound `msg.cemId` actually carries, since the RM's own session is always keyed by that placeholder; (2) `Connect`/`Disconnect` transport events now also publish a debug-sidebar entry (topic suffixed `(Connect)`/`(Disconnect)`) for `s2-dbus`, `s2-websocket`, and `s2-resource`'s both transports - previously only `Message` traffic was visible there, so a CEM connecting/disconnecting was invisible without checking the runtime log. New/updated tests in `test/nodes/s2-dbus.test.ts`, `test/nodes/s2-websocket.test.ts`, `test/nodes/s2-resource.test.ts`
- [x] 4.11 Run `npm test` and confirm lint + full suite pass: 417/417 tests, lint clean
