## 1. ModeInstruction gains a `values` convenience field

- [x] 1.1 Add failing tests in `test/lib/ombc-controller.test.ts` for a `values` field on the emitted `ModeInstruction` payload: a plain number for a 3-phase-symmetric mode, and a `[L1, L2, L3]` array for a per-phase mode, for a range instruction at factor 0, 1, and a fractional factor. (`OMBCController` has no dedicated unit test file - it's covered via `test/nodes/s2-ombc.test.ts`'s existing "instruction resolution" describe block, so the new tests were added there instead, matching the codebase's existing convention.)
- [x] 1.2 Implement a `commodityPowerToValues()`-style conversion in `src/lib/s2/ombc-controller.ts`, branching the same way `calculatePower()` already distinguishes symmetric vs. per-phase, and include its result as `values` in `handleInstruction()`'s emitted payload alongside the existing `commodityPower`. (Implemented as `calculateValues()`, computed independently from the power ranges/factor rather than derived from `commodityPower`'s already-rounded per-phase output, to avoid a rounding mismatch for the symmetric case - see the new test asserting `values: 2000` vs. `commodityPower`'s `667*3=2001`.)
- [x] 1.3 Run `npm test` and confirm the new tests pass and existing `ModeInstruction`-shape tests are unaffected (payload keeps `id`/`index`/`label`/`factor`/`commodityPower` unchanged, `values` is additive). All 42 tests in `test/nodes/s2-ombc.test.ts` pass.
- [x] 1.4 Update the `ModeInstruction` shape documented in `src/nodes/s2-ombc/index.ts`'s header JSDoc to mention `values`.
- [x] 1.5 Update README's OMBC/My-PV AC-Thor example section to mention `values` as the simplest way to feed the instructed power into a `PowerMeasurement` input. (No such section existed yet - added a new "Faking a measurement from an OMBC instruction" subsection under "Sending power measurements over D-Bus".)

## 2. ModeConfirmation accepts a whole ModeInstruction payload

- [x] 2.1 Add failing tests in `test/lib/ombc-controller.test.ts` for `resolveModeIdentifier()`/confirm handling: `id`+`index`+`label` all present resolves via `id`; `index`+`label` present (no `id`) resolves via `index`; a confirm payload that is exactly a prior `ModeInstruction` payload (`{id, index, label, factor, commodityPower, values}`) resolves correctly and uses its `factor`; a confirm with none of `id`/`index`/`label` (new or legacy field names) is still rejected; an unresolvable `id` is still rejected even when `index`/`label` are also present. (Added to `test/nodes/s2-ombc.test.ts`'s "confirm mode" describe block, same as task 1.1's note.)
- [x] 2.2 Change `resolveModeIdentifier()` in `src/lib/s2/ombc-controller.ts` from an exclusivity check (`providedCount !== 1`) to a priority pick (`id` > `index` > `label`), keeping legacy field-name support unchanged.
- [x] 2.3 Run `npm test` and confirm all `s2-ombc`/`OMBCController` confirm-message tests pass, including the existing "no identifier supplied" and "unknown id"/"label matches none/multiple" error-path tests. **Correction to this task's own premise**: the existing file *did* have a "rejects a confirm with zero or multiple mode identifiers" test exercising the old exclusivity behavior - split into "rejects a confirm with zero mode identifiers" (kept) and a new "resolves via id when id and index are both present, ignoring index" test (replacing the old "multiple -> error" assertion, since that's no longer an error). All 47 tests in `test/nodes/s2-ombc.test.ts` and the full suite (476/476) pass.

## 3. Friendly editor: optional power modulation per mode

- [x] 3.1 In `resources/s2-ombc-editor.js`, add a `modulate` boolean to the friendly-mode data model (default `false`), and per-mode "from"/"to" value fields (`valueSymmetricFrom`/`valueSymmetricTo`, `valueL{1,2,3}From`/`valueL{1,2,3}To`) alongside the existing single-value fields.
- [x] 3.2 In `addModeItem()`, add a "Support power modulation" checkbox per mode row; toggling it swaps the single value input(s) for from/to pairs (mirroring the existing `symCheckbox`/`updateSymVisibility()` pattern for symmetric vs. per-phase). (Renamed to `updateValueVisibility()` since it now switches over two independent booleans - symmetric and modulate - showing exactly one of four value-input groups.)
- [x] 3.3 In `getFriendlyModes()`, read the new checkbox and from/to fields back into the plain-object shape.
- [x] 3.4 In `friendlyStateToSystemDescription()`, when `modulate` is true use the from/to values as `start_of_range`/`end_of_range`; when false, keep today's behavior (single value for both bounds).
- [x] 3.5 Relax `isFriendlyPowerRanges()` to accept `start_of_range !== end_of_range` (dropping the equality requirement) while keeping every other structural check (exactly 1 symmetric range, or exactly 3 per-phase ranges covering L1/L2/L3 and nothing else).
- [x] 3.6 In `systemDescriptionToFriendlyState()`, populate the new from/to fields from `start_of_range`/`end_of_range` and set `modulate: start_of_range !== end_of_range`. Verified 3.1-3.6 with a standalone Node script exercising the pure conversion functions directly (fixed/modulating, symmetric/per-phase, and round-trip cases) - no jest coverage exists for this file (see 4.6's note), so this was ad hoc, not added to the suite.
- [x] 3.7 Update `s2-ombc-config/index.html`'s and `s2-resource/index.html`'s help text: remove "a genuine power range" from the list of things that force Advanced mode, and mention the new "Support power modulation" option under Friendly mode instead. Also updated both nodes' `ModeConfirmation` payload documentation (tooltip and help text) to reflect task group 2's priority resolution + optional `factor`, which had been missed in `s2-resource/index.html` specifically.
- [x] 3.8b **User-reported UI fix** (found via manual testing, not part of the original task list): with "Support power modulation" on for a per-phase mode, the inactive phase(s) on a phase-constrained device (`setActivePhase()` - e.g. a single-phase D-Bus device wired to just L2) were shown dimmed-but-still-visible rather than hidden, doubling to six visible inputs and making the row too wide. Changed `setActivePhase()` in `resources/s2-ombc-editor.js` to hide (`.toggle()`) inactive phase groups instead of just dimming them via CSS opacity, for both the fixed-value and modulating layouts; removed the now-unused `.s2-ombc-mode-phase-dimmed` CSS rule from `resources/s2-styles.css`. `npm test` (476/476) and `npm run build` still pass.
- [x] 3.8 Live-verified in a running Node-RED instance (via Chrome browser automation, once reconnected mid-session - see conversation): opened the pre-existing "ombc test" `s2-ombc-config` node, checked "Support power modulation" on its per-phase "On" mode (rendered From/To pairs correctly for all 3 phases), set L1 to 200-3000, switched to Advanced (JSON) - confirmed `start_of_range: 200, end_of_range: 3000`, switched back to Friendly - stayed representable with modulation still checked and values intact (byte-exact round-trip). Also confirmed on `s2-resource`'s embedded Control tab (phase-constrained to L2 via its D-Bus config): only L2's From/To pair rendered, L1/L3 fully hidden (not just dimmed) - see 3.8b.

## 4. s2-resource: stop silently discarding a non-representable OMBC configuration

- [x] 4.1 Reproduced against the *fixed* code directly (the pre-fix discard behavior was already established via code reading earlier in the conversation, and the fix was written test-first per the delta spec's scenarios - no separate before/after run against unpatched code was done, since Node-RED wasn't live until after the fix was already written). Live, via a real `s2-resource` node ("Bug repro L2", `Transport: D-Bus`, phase-constrained to L2): injected a hand-crafted non-representable `systemDescription` (two modes, only one of the two required transitions - `isFriendlyRepresentable` correctly returns `false`) directly into the node's model via the browser console, then Done + Deploy.
- [x] 4.2 In `src/nodes/s2-resource/index.html`, add the same Friendly/Advanced tab-bar and switch logic `s2-ombc-config/index.html` already has (same `s2-tabs`/`s2-tab`/`s2-tab-active` classes, same `isFriendlyRepresentable()`/`friendlyStateToSystemDescription()`/`systemDescriptionToFriendlyState()` calls) above `#s2-resource-ombc-mode-list`, replacing the current unconditional `else { addItem(defaultStandbyMode()) }` fallback in `oneditprepare` with "show Advanced mode with the existing JSON" - matching `s2-ombc-config`'s own `oneditprepare` structure. Also extended the fallback to cover outright invalid JSON the same way `s2-ombc-config` already does (only a truly empty `systemDescription` still defaults to a fresh Standby mode) - the task as written only mentioned "not friendly-representable", but the identical silent-discard risk existed for malformed JSON too.
- [x] 4.3 Change `oneditsave` to rebuild `systemDescription` from friendly state only when Friendly mode is currently active (mirroring `s2-ombc-config`'s own `oneditsave` guard), leaving Advanced-mode JSON untouched on save. **Correction to this task's own premise**: matching `s2-ombc-config`'s actual mechanism required writing the computed JSON into the `#node-input-systemDescription` typedInput (`$(...).typedInput('value', ...)`), not directly to `this.systemDescription` - Node-RED's own automatic property extraction reads every `defaults` field from its matching `#node-input-<name>` element *after* `oneditsave` returns, which would otherwise silently overwrite a direct assignment with the typedInput's stale prior value. Confirmed by re-reading `s2-ombc-config`'s own `oneditsave`, which does the same and says so in its own comment.
- [x] 4.4 Update `s2-resource/index.html`'s help text for the OMBC control type to mention the Friendly/Advanced toggle, matching `s2-ombc-config`'s help text. Also updated the "Default mode" dropdown's `refreshDefaultModeOptions()` to read from the Advanced-mode JSON (parsed) when Advanced mode is active, instead of always reading the (now possibly stale/irrelevant) friendly editableList - an adjacent silent-corruption risk for `defaultOperationModeId` specifically, discovered while wiring the tab toggle, not called out in the original task text.
- [x] 4.5 Live-verified: reopened the node from 4.1 - Control tab opened directly into **Advanced (JSON)**, showing the injected JSON unchanged (confirmed byte-exact via a console read, char-code-verified to rule out a display artifact), not a default "Standby/off" mode. "Default mode" dropdown correctly showed "None" (the previously-selected mode id no longer existed in the new JSON) rather than erroring or silently pointing at the wrong mode. Clicking the "Friendly" tab showed the same warning banner text `s2-ombc-config` uses ("...a non-standard commodity, a blocked transition, or a timer not matching the per-mode minimum-duration pattern...") and correctly stayed in Advanced mode - full parity confirmed. Also re-verified the happy path: restored the node to a friendly-representable two-mode config (fully-connected transitions), which opened and saved cleanly in Friendly mode with L1/L3 still hidden (single-phase device), no regression.
- [x] 4.6 No jest/jsdom test is added for this (same gap noted in task group 3 - `resources/*.js` and `*/index.html` have no automated coverage in this repo); live verification in 4.1/4.5 is the regression check. `npm run build` and the full `npm test` suite (476/476) pass with these changes.

## 5. Wrap-up

- [x] 5.1 Run `npm test` (full suite) and `npm run build`. Both pass (476/476 tests).
- [x] 5.2 Add a CHANGELOG.md entry under `## [Unreleased]` for all four changes, including the `s2-resource` data-loss fix under a `### Fixed` heading with a note that a configuration already silently discarded before this fix ships cannot be recovered.
- [x] 5.3 Ask the user whether to run `/opsx:archive` (syncs this delta spec into `openspec/specs/control-type-ombc/spec.md`) once implementation and live verification are both confirmed. User ran `/opsx:archive` directly.
