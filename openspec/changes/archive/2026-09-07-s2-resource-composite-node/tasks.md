

## 1. Extract s2-rm's session logic

- [x] 1.1 Write/port tests for the extracted class against `test/nodes/s2-rm.test.ts`'s existing scenarios (handshake, control-type selection, instruction ack/routing, `S2/0/Active` signal, generic `UpdateStatus`/`SystemDescription` commands) so both the class and the `s2-rm` wrapper are covered
- [x] 1.2 Implement `S2ResourceManager` in `src/lib/s2/resource-manager.ts`: same behavior as today's `registerType` body, callback-based ("send to transport", "emit to CEM output") instead of `node.send()`
- [x] 1.3 Rewrite `src/nodes/s2-rm/index.ts`'s `registerType` as a thin wrapper constructing `S2ResourceManager` and wiring its callbacks to the node's two ports
- [x] 1.4 Run `test/nodes/s2-rm.test.ts` unchanged and confirm it still passes against the wrapper

## 2. Extract the shared OMBC friendly-editor widget

- [x] 2.1 Move the friendly-editor functions (`addModeItem`, `defaultStandbyMode`, friendly/advanced conversion) out of `src/nodes/s2-ombc-config/index.html` into a shared client-side script loaded by both `s2-ombc-config` and `s2-resource` (`resources/s2-ombc-editor.js`, `window.__s2OmbcEditor`)
- [x] 2.2 Remove the "Abnormal only" checkbox and `abnormalConditionOnly`/`abnormal_condition_only` field from the widget; on load, coerce any existing `true` value to `false` (the field is simply no longer read/round-tripped by friendly mode, so it's dropped on the next friendly-mode save)
- [x] 2.3 N/A - no automated test exists for this (or any) node's client-side editor HTML in this codebase (Jest only covers `src/**/*.ts`); `test/nodes/s2-ombc.test.ts`'s `abnormal_condition_only: false` fixtures are unrelated server-side OMBCSystemDescriptionConfig fixtures, not editor-widget tests, and are untouched
- [x] 2.4 Verified statically: `npm run build` succeeds, `resources/s2-ombc-editor.js` is a straight extraction of the pre-existing logic (checkbox rows and `abnormalConditionOnly` reads/writes removed) with no other behavior change. Not verified in a live browser/Node-RED editor session - flag if you want that done before merging

## 3. s2-resource: Connection + Resource Manager tabs

- [x] 3.1 Write tests for RM identity handling (resourceId auto-generation, roles) reusing `s2-rm-config`'s existing test patterns, now inline in `s2-resource`
- [x] 3.2 Write tests for `Transport: WebSocket` wiring to an `s2-cem-config` reference, and `Transport: External` exposing the transport-in/out ports with `s2-rm`-identical message shapes
- [x] 3.3 Implement `src/nodes/s2-resource/index.ts`: constructs `S2ResourceManager`, wires transport slots to either the built-in WebSocket client (reusing `S2WebSocketTransport` from `lib/transport/websocket.ts`, same class `s2-websocket` uses) or external ports per `Transport`
- [x] 3.4 Implement `src/nodes/s2-resource/index.html`'s Connection and Resource Manager tabs (RM name, resourceId, roles; Transport select; CEM config-node picker with edit pencil). Deviation from the mockup: dropped the fictitious "Keep-alive" field (no such config exists on `s2-websocket` today - it hardcodes `keepAliveInterval: 0`) in favor of `s2-websocket`'s real config surface, "Reconnect interval" and "Log raw S2 messages"

## 3b. s2-resource: Transport: D-Bus (added during implementation, see design.md and proposal.md)

- [x] 3b.1 Write tests for `Transport: D-Bus` constructing `S2DbusTransport` from an `s2-dbus-config` reference, with output/input message parity to `s2-dbus`
- [x] 3b.2 Implement the D-Bus wiring path in `src/nodes/s2-resource/index.ts`, mirroring the WebSocket path (construct `S2DbusTransport`, wire its events to the RM's callbacks instead of external ports). Deliberately narrower than the standalone `s2-dbus` node: no power-measurement relay - documented in the node's own doc comment and help panel. Uses this node's own id for the D-Bus service name (`nodeId`, not the S2 resourceId) - see `2026-09-06-s2-dbus-transport`'s design.md for why
- [x] 3b.3 Add `Transport: D-Bus` to `src/nodes/s2-resource/index.html`'s Connection tab: an `s2-dbus-config` node-picker (device type/instance/connection mode live there, matching the CEM-picker pattern for WebSocket)
- [x] 3b.4 Updated `docs/S2_REFERENCE.md`'s `s2-resource` mention and this change's proposal/design/specs to reflect the D-Bus option landing in this change rather than as a later follow-up

## 4. s2-resource: Control Type tab

- [x] 4.0 (added during implementation, see design.md) Extract `s2-ombc`'s server-side logic into `OMBCController` (`src/lib/s2/ombc-controller.ts`), same pattern as `S2ResourceManager`; rewrite `s2-ombc/index.ts` as a thin wrapper; `test/nodes/s2-ombc.test.ts` passes unchanged
- [x] 4.1 Write tests for `Control type: OMBC` wiring the built-in operation-mode config into `S2ResourceManager`'s CEM-output path, and `Control type: None` exposing the CEM-in/out ports with `s2-rm`-identical message shapes
- [x] 4.2 Implement the OMBC wiring path in `src/nodes/s2-resource/index.ts` by constructing `OMBCController` directly, reusing the shared widget's saved-`systemDescription` shape (no `s2-ombc-config` node instance)
- [x] 4.3 Implement `src/nodes/s2-resource/index.html`'s Control Type tab: Control type select, embedded OMBC friendly editor (via the shared script from task 2.1), "None" panel matching the mockup's explanatory text

## 5. Port contract and dynamic ports

- [x] 5.1 Write a test asserting `Transport: External` + `Control type: None` produces the exact same port count, order, and message shapes as `s2-rm` (per the `s2-resource` spec's port-contract requirement)
- [x] 5.2 Implement `oneditsave` port recomputation - revised during implementation (see design.md): only `node.outputs` is ever recomputed, from `Transport` alone; `node.inputs` is a fixed 1, so there is no "last-deployed state" comparison to make for it

## 6. Integration and docs

- [x] 6.1 Register `s2-resource` in `package.json`'s `node-red.nodes` map
- [x] 6.2 Update `docs/S2_REFERENCE.md` to introduce `s2-resource` as the recommended starting point, with `s2-rm` + individual nodes kept as the manually-wired/advanced path
- [x] 6.3 Add an example flow (`examples/s2-resource-quickstart.json`) showing the 6-node OMBC quickstart collapsed to `s2-resource` alone (`Transport: WebSocket`, `Control type: OMBC`)
- [x] 6.4 Run `npm test` and confirm lint + full suite pass (re-run after the `Transport: D-Bus` addition above): 373/373 tests, lint clean

## 7. Fixes from actual editor use (added post-implementation, see design.md)

- [x] 7.1 Extract `s2-dbus`'s power-measurement cache into `PowerMeasurementCache` (`src/lib/s2/power-measurement-cache.ts`); refactor `s2-dbus/index.ts` to use it (`test/nodes/s2-dbus.test.ts` passes unchanged)
- [x] 7.2 Add the power-measurement relay to `s2-resource`'s `Transport: D-Bus` path (input dispatch + `routeToDbusTransport`, looping `PowerMeasurement` commands back into `S2ResourceManager.handleInput`); new tests in `test/nodes/s2-resource.test.ts`
- [x] 7.3 Make the Resource Manager tab's Control Types checklist conditional on `Control type: None`; `oneditsave` advertises `OPERATION_MODE_BASED_CONTROL` automatically when `Control type: OMBC` instead of reading the (now hidden) checkboxes; dropped the OMBC option from that checklist
- [x] 7.4 Run `npm test` and confirm lint + full suite pass: 378/378 tests, lint clean
- [x] 7.5 (added post-implementation, see `2026-09-06-s2-dbus-transport`'s design.md) Expose power measurement as a real D-Bus BusItem property in `s2-resource`'s `Transport: D-Bus` path too, via `S2DbusTransport`'s new `measurementType` option and `setMeasurementValues()` - passes `dbusConfig.measurementType` through, pushes every cached update regardless of S2 activity
- [x] 7.6 Run `npm test` and confirm lint + full suite pass: 387/387 tests, lint clean
- [x] 7.7 (found via real D-Bus inspection with a live Venus system) Fix the same Control-Types-style drift for power measurement: `rmDetails.providesPowerMeasurementTypes` now derives from the `s2-dbus-config` node's `measurementType` when `Transport: D-Bus` (not the Resource Manager tab's own Power Meas. field, which is now hidden in that mode with a hint pointing at the Connection tab). Moved the `s2-dbus-config` lookup earlier in `src/nodes/s2-resource/index.ts` so it's resolved once, before `rmDetails` is built, instead of a second time inside the `Transport: D-Bus` setup block. New tests in `test/nodes/s2-resource.test.ts`
- [x] 7.8 Run `npm test` and confirm lint + full suite pass: 389/389 tests, lint clean
- [x] 7.9 (found via a real CEM connecting to a deployed `s2-resource` with `Transport: D-Bus`; see `2026-09-06-s2-dbus-transport`'s design.md/tasks.md for the actual fix) `S2DbusTransport.setActive()` threw `Property S2/0/Active not found in properties.` because that property (and `S2/0/Rm`) were never declared - fixed entirely inside the shared `S2DbusTransport` class, so `s2-resource` needed no code changes of its own; covered by `test/lib/dbus-transport.test.ts`
- [x] 7.10 Run `npm test` and confirm lint + full suite pass: 392/392 tests, lint clean
- [x] 7.11 (requested after real CEM testing surfaced the "Pre-connection default status" gap - see design.md) Add a "Default mode" dropdown to the Control Type tab's OMBC section, backed by a new `defaultOperationModeId` config field: `oneditprepare` keeps its options synced to the live mode-editableList (structural add/remove/reorder, and label edits via input delegation), preserving the selection across a refresh where possible. At deploy time, `s2-resource/index.ts` feeds it into `OMBCController.handleInput` as a `confirmedOperationModeId` confirm (no `cemId`) right after constructing the controller - the same effect a manually-wired Inject node produces, just automatic. New tests in `test/nodes/s2-resource.test.ts`
- [x] 7.12 Run `npm test` and confirm lint + full suite pass: 394/394 tests, lint clean
- [x] 7.13 (found via real CEM testing - see design.md) Add "Auto-confirm mode instructions" checkbox to the OMBC tab (`autoConfirmInstructions`, on by default): `OMBCController` gains an `autoConfirmInstructions` option and a shared `confirmMode()` helper (extracted from `handleConfirm`'s tail, also called from `handleInstruction` when the option is set), so an instructed mode change is confirmed back immediately instead of waiting indefinitely for a `ModeConfirmation` the flow never sends. Left off by default in the shared class itself and not wired into `s2-ombc`/`s2-ombc-config` - `s2-resource` opts in explicitly (`config.autoConfirmInstructions !== false`), so standalone `s2-ombc` usage is unchanged. New tests in `test/nodes/s2-resource.test.ts`
- [x] 7.14 Run `npm test` and confirm lint + full suite pass: 396/396 tests, lint clean
- [x] 7.15 (requested for debugging usability; see `2026-09-06-s2-dbus-transport`'s design.md for the underlying mechanism) `s2-resource`'s `config.debug`-gated raw S2 message trace, for both `Transport: WebSocket` and `Transport: D-Bus`, now goes to the Node-RED editor's Debug sidebar (`publishToDebugSidebar()`, shared with `s2-dbus`/`s2-websocket`) instead of `node.log()`. Checkbox relabeled "Show raw S2 messages in debug sidebar". New tests in `test/nodes/s2-resource.test.ts`
- [x] 7.16 (requested for clarity - "make it more clear what type of message this is about... end users are probably new to S2") Relabeled the OMBC tab's "Auto-confirm mode instructions" checkbox to "Auto-confirm mode-switch requests (OMBC.Instruction)", naming the actual S2 message type in both the label and tooltip so it's recognizable against what the Debug sidebar (task 7.15) now shows and what the S2 spec itself calls it
- [x] 7.17 Run `npm test` and confirm lint + full suite pass: 409/409 tests, lint clean
- [x] 7.18 (found via real use of the debug sidebar from tasks 7.15/4.7; see `2026-09-06-s2-dbus-transport`'s design.md/tasks.md for the shared cemId/Connect-Disconnect fix) `s2-resource`'s debug-sidebar entries (both transports) now include the cemId in their topic, and its D-Bus path tracks the real D-Bus-supplied cemId for outbound entries instead of the internal placeholder. `Connect`/`Disconnect` transport events now also publish a debug-sidebar entry, for both `Transport: WebSocket` and `Transport: D-Bus`. New/updated tests in `test/nodes/s2-resource.test.ts`
- [x] 7.19 (requested for S2 lingo clarity, in `OMBCController` - shared with `s2-ombc`, not `s2-resource`-specific) `ModeInstruction`'s resolved payload: `power` (a plain `[L1, L2, L3]` number array) renamed to `commodityPower`, restructured as an array of `{ commodity_quantity, value }` pairs - the same shape S2's own PowerMeasurement/PowerRange values use elsewhere in this codebase, per the user's explicit choice between a plain rename and this S2-shaped restructure. `rawMessage` renamed to `rawS2Message` for clarity that it's the original S2 protocol message, not an arbitrary "raw" blob. Updated `test/nodes/s2-ombc.test.ts`, `src/nodes/s2-ombc/index.ts`/`index.html` docs, and `examples/opportunity-loads-sg-ready-ombc.json` (which read `payload.power` and indexed it as a plain number)
- [x] 7.20 (requested - "The node-red way is not to have it given a default name") `s2-resource`'s `rmName` field no longer defaults to `'RM: Virtual'` in the editor (now `''`, matching the existing placeholder-text pattern), and the node's canvas label no longer falls back to `rmName` when unnamed - just `this.name || 's2-resource'`, the standard Node-RED convention. The runtime constructor's own `config.rmName || 'RM: Virtual'` fallback (feeding `ResourceManagerDetails.name`) is unchanged - a real S2 protocol field still needs a non-empty value even if the user never sets one, which is a different concern from the editor pre-filling a name for the user
- [x] 7.21 Run `npm test` and confirm lint + full suite pass: 417/417 tests, lint clean
- [x] 7.22 (found via real testing - "I thought the ring would be green, but it is gray... most of the time it falls back to grey") Fixed a genuine status bug, not a rendering glitch: `S2ResourceManager`'s own `updateStatus()` unconditionally reports grey `'waiting for CEM'` whenever its session count returns to 0 - correct for `s2-rm`/external transports (which have no better information), but wrong for `s2-resource`'s built-in transports once registered/connected, where "no CEM currently connected" should still read as green ("ready and listening"), not the same grey the node starts in before the transport has done anything. Fixed by tracking a `transportReady` flag (set on `open` for both transports; cleared on WebSocket's `close`, since that's a real disconnect - not cleared on D-Bus `disconnect`, since the D-Bus service itself stays registered independent of any one CEM session) and wrapping `rm`'s `onStatus` callback to upgrade grey `'waiting for CEM'` to green while `transportReady` is true. New tests in `test/nodes/s2-resource.test.ts` covering both the D-Bus stay-green-after-disconnect case and the WebSocket correctly-grey-while-reconnecting case
- [x] 7.23 Run `npm test` and confirm lint + full suite pass: 419/419 tests, lint clean
- [x] 7.24 (requested - "Now in the control type pane, I can still set the values for the other phases. I'd like the ones that aren't selected to be greyed out") For `Transport: D-Bus` with a single-phase (`nrOfPhases: 1`) `s2-dbus-config`, the OMBC tab's per-phase L1/L2/L3 fields now dim and disable whichever phases don't match the config's `phaseSetting` ("Wired to"), since a single-phase device never actually reports those. Implemented in the shared editor widget (`resources/s2-ombc-editor.js`): each phase's label+input is now wrapped in its own `.s2-ombc-mode-phase-l<n>` group (previously just loose text nodes, with no element to target), plus a new `setActivePhase(listSel, activePhase)` export that dims/disables every group not matching. `s2-resource/index.html` calls it (re-reading the referenced `s2-dbus-config` node's live values, so edits made in its own sub-dialog are picked up) on `Transport` change, `s2-dbus-config` selection change, switching to the Control Type tab, and whenever a mode row is added. `s2-ombc-config`/`s2-ombc` never call it (no phase-wiring information exists there), so their fields are unaffected. No automated test - matches this codebase's existing "no test harness for client-side editor HTML" limitation (noted in task 2.3); verified via `npm run build` + a manual syntax check of the edited script blocks
- [x] 7.25 Run `npm test` and confirm lint + full suite pass: 419/419 tests, lint clean (no server-side code touched)
- [x] 7.26 (requested - "The (i) is overlapping the text") Fixed the tooltip icon overlapping checkbox label text on the "Show raw S2 messages in debug sidebar" and "Auto-confirm mode-switch requests (OMBC.Instruction)" checkboxes (and, latently, every `.s2-checkbox-label` tooltip across the codebase, e.g. `s2-rm-config`'s role/control-type checkboxes - previously masked there by short label text). Root cause: `.tooltip-icon`'s `position: absolute; right: 2px` assumed it always lives inside `.s2-form .form-row label`, the only selector that reserved `position: relative` + padding for it - `.s2-checkbox-label` never got the same treatment, so the icon anchored to whatever positioned ancestor happened to be further up the DOM instead of its own label. Fixed by giving `.s2-checkbox-label` the same `position: relative` + `padding-right: 20px` reservation, in `resources/s2-styles.css`
- [x] 7.27 Run `npm test` and confirm lint + full suite pass: 419/419 tests, lint clean (CSS-only change)
- [x] 7.28 (follow-up - task 7.26's fix didn't visibly help; user asked to rule out caching first) Added a temporary, unmistakable "lime border" debug marker to `.s2-checkbox-label` and had the user hard-refresh to confirm the edited stylesheet was actually being served - it was (the border showed up), which ruled out caching/stale-deployment and pointed back at the CSS itself. That surfaced the *actual* root cause task 7.26 missed: the checkbox's `<label>` sits inside a `<div class="form-row ...">`, so `.s2-form .form-row label`'s `width: 120px !important` (higher specificity - it matches on the `label` element type, which `.s2-form .s2-checkbox-label`'s class-only selector doesn't) was silently winning over `.s2-checkbox-label`'s own `width: auto !important` the whole time, clipping the label to 120px regardless of 7.26's `position: relative` addition - the visible text overflowed past that clipped box, and the tooltip icon (correctly positioned *relative to the clipped box*) landed wherever within it rather than at the end of the actual text. Fixed using the exact same specificity-tie-break trick this file already documents and uses for `label.s2-section-label` above it: changed the selector to `.s2-form label.s2-checkbox-label` (adding the `label` element type) so it ties `.form-row label`'s specificity and wins on source order. Removed the temporary lime-border marker. This also silently affects (and fixes) `s2-rm-config`'s role/control-type checkboxes and this node's own Roles/Control-Types grids - previously always rendering at the wrong 120px instead of stretching to their 116px grid cell, just never noticed since the two widths are close enough to look right by coincidence for short labels
- [x] 7.29 Run `npm test` and confirm lint + full suite pass: 419/419 tests, lint clean (CSS-only change)
- [x] 7.30 (follow-up - "even though it still feels a bit far to the right side") After 7.28's fix, the icon moved from "mid-sentence" to "the far right edge of the whole row" - `.s2-form label.s2-checkbox-label` used `display: flex` (block-level, stretches to its container's full width) where the pre-existing, otherwise-identical `label.s2-ombc-mode-checkbox-label` a little further down this same file correctly uses `display: inline-flex` (shrink-to-fit). Once the 120px clip was gone, the label was free to actually stretch full-width per `flex`'s real block-level behavior, putting the absolutely-positioned icon (`right: 2px`) at that far edge instead of snug against the label's own text. Changed to `inline-flex` to match its sibling rule. (Separately noted: this does not explain the still-open "edit panel is too wide" observation - inline-flex vs flex only changes how far *within* its row the label stretches, not how wide the row/dialog itself is; that remains unresolved, most plausibly Node-RED's own per-node-type remembered/resizable tray width rather than anything in this stylesheet)
- [x] 7.31 Run `npm test` and confirm lint + full suite pass: 419/419 tests, lint clean (CSS-only change)
- [x] 7.32 (resolves the "edit panel is too wide" observation from tasks 7.24/7.26/7.30 - root-caused via the user's own DevTools inspection: a fresh node was narrow, only an existing node with `Transport: D-Bus` was stuck wide, and the wide element's computed width had no round-number match anywhere in this stylesheet) Node-RED's own editor CSS sets `code { white-space: nowrap }` globally. The only `<code>` element inside the actual edit-form template (not the separate help panel, which isn't part of the resizable tray) is the D-Bus Connection tab's "Feed a power reading..." hint, embedding a full JSON snippet as prose - forced onto one unbreakable line by that global rule, which is what the tray's own auto-sizing-on-open apparently measures against. Fixed with `.s2-form code { white-space: normal }`, letting that snippet wrap like the rest of the sentence around it. Scoped to `.s2-form` per this file's own convention, so the help panel's `<code>` styling (nowrap, Node-RED's default) is untouched - it's a separate, non-resizable area where that's fine
- [x] 7.33 Run `npm test` and confirm lint + full suite pass: 419/419 tests, lint clean (CSS-only change)
- [x] 7.34 (7.32's fix wasn't the full story - "on re-opening after deploying, it is still wide again", user's own suspicion: "Isn't it the s2-resource-ombc-mode-list's min-width value that is plaging us here?") Correct: `#s2-resource-ombc-mode-list` (and `s2-ombc-config`'s equivalent) was given an explicit `min-width: 480px` via `$(listSel).css(...)`. That panel is `display:none` until the Control Type tab is actually opened - which happens as soon as anyone configures an OMBC mode, i.e. routinely - and once visible, the dialog has to grow to fit that 480px floor. Node-RED then appears to remember/restore that grown width for that node instance on subsequent opens, even back on the (narrower) Connection tab. Removed the `.css('min-width', '480px')` call from both `s2-resource/index.html` and `s2-ombc-config/index.html` (kept `min-height`, which only affects the editableList's own vertical sizing) - the mode row's per-phase L1/L2/L3 inputs (`.s2-ombc-mode-value-wrap`, `white-space: nowrap`) already have their own intrinsic minimum width from their content, and `.s2-ombc-mode-row`'s existing `flex-wrap: wrap` lets the rest of the row's fields wrap onto additional lines on a narrower dialog instead of forcing one. 7.32's `<code>` fix is unrelated but still valid and kept - both were genuine, independent width-forcing issues
- [x] 7.35 Run `npm test` and confirm lint + full suite pass: 419/419 tests, lint clean (CSS/HTML-only change)
- [x] 7.36 (7.34 still wasn't the full story - "Unfortunately it doesn't work still... Isn't there another (min-)width somewhere?") Found via the user's own careful DevTools A/B testing (comparing `.s2-form`'s own computed width, the same element both times): reopening a deployed node with `Transport: WebSocket` (the default) reliably measures 538.258px; reopening one with `Transport: dbus` or `Transport: external` reliably measures 958.812px - identical, byte-for-byte, to measurements taken *before* tasks 7.32 and 7.34's fixes. Since our own content changes had zero effect on that number, it isn't being computed from our current content at all: Node-RED itself applies an inline `min-width` directly on `.red-ui-tray-body` (visible in the user's own DevTools inspection: `style="min-width: 539.453px; ..."`), apparently once per some state key rather than freshly per open - exhaustive searching of Node-RED core's minified source did not turn up the exact originating calculation, but it does not need to be found: an `!important` rule in an external stylesheet is the one case that overrides a plain (non-`!important`) inline style, so `.red-ui-tray-body:has(.s2-form) { min-width: 0 !important }` neutralizes it directly, letting our own content's genuine sizing (already correct per 7.32/7.34) take over. `:has()` scopes the override to only trays containing our own form - Node-RED 4.1.10 (confirmed running) fully supports it, and no other node type's dialog is affected
- [x] 7.37 Run `npm test` and confirm lint + full suite pass: 419/419 tests, lint clean (CSS-only change)
- [x] 7.38 (7.36's `:has()`-based override "still doesn't work") `:has()` needs a fairly recent browser (Chrome 105+/2022, Firefox 121+/2023) - on anything older it silently matches nothing, so the whole rule becomes a no-op with no visible sign of failure, which fits "still doesn't work" without contradicting the `!important`-beats-inline-style reasoning (that part was never actually exercised if the selector never matched). Replaced with a `:has()`-free equivalent: `s2-resource/index.ts`'s `oneditprepare` finds the tray body via `$('#node-input-transport').closest('.red-ui-tray-body')` and adds a marker class (`s2-narrow-tray`); `s2-styles.css` targets `.red-ui-tray-body.s2-narrow-tray { min-width: 0 !important }` instead of the `:has()` selector - same override mechanism, no modern-CSS-selector dependency. Removed again in both `oneditsave` and a new `oneditcancel` so it never leaks onto whatever node type's dialog opens next in the same (apparently reused) tray body element - a real risk this class-based approach introduces that `:has()` didn't have, since `:has()` re-evaluates live against whatever's currently in the DOM rather than a class we'd have to remember to clean up
- [x] 7.39 Run `npm test` and confirm lint + full suite pass: 419/419 tests, lint clean (CSS/HTML-only change)
